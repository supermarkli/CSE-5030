2026-03-10
- Added `test.S` bare-metal RISC-V array summation program and `link.ld` for lab Step 1.2.
2026-03-12
- Audited `todo.md` against local lab/chipyard progress and appended remaining PDF tasks in Chinese.
- Refined `todo.md` into per-step checklist format and added involved file paths for each step.

2026-03-12
- Added `note.md` with beginner-oriented background and concept notes based on `week2_lab.pdf`.
- Added `hello.c` for Lab 2 Step 2 and created `report.md` with Step 1.3/Step 2 analysis.
- Updated `todo.md` with completed Step 1.3, Step 2, and submission-preparation status.
- Rewrote `report.md` into a polished Chinese submission draft with screenshot placeholders.
- Added screenshot instructions to `report.md` for placeholders 1-7, including directories, commands, and capture suggestions.
- Trimmed `report.md` to a more concise submission-focused version while keeping all PDF-required items.
- Compressed `report.md` further by shortening ISA explanations and reducing the assembly excerpt to the minimum needed.
- Corrected the `main` call-site calling-convention explanation in `report.md` to distinguish caller actions from function prologue/epilogue.
- Strengthened the compiler-flag analysis in `report.md` with explicit bare-metal/Verilator rationale.
- Added explicit screenshot captions to `report.md` for the `make`, `tail test.out`, and `whoami` evidence.
- Refined the `main` calling-convention explanation in `report.md` to state clearly why `ra` and `s0` are saved/restored.
- Corrected the Step 1.2 screenshot captions in `report.md` to match the actual terminal and trace images.
- Tightened the `main` call-site explanation in `report.md` to distinguish call-site behavior from function prologue/epilogue saves.
- Softened the `-fvisibility=hidden` and `-o hello.elf` flag analysis in `report.md` to avoid overstating necessity for Verilator.
